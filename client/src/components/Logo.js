const Logo = (props) => (
  <img
    alt="Logo"
    src="/static/vit.svg"
    style={{
      height: '42px',
      width: '42px',
      // fill: '#fff'
      // color: '#fff'
    }}
    {...props}
  />
);

export default Logo;
